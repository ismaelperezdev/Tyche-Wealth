global:
  scrape_interval: 15s

scrape_configs:
  - job_name: user-service
    metrics_path: /actuator/prometheus
    basic_auth:
      username: prometheus-scraper
      password: __PROMETHEUS_PASSWORD__
    static_configs:
      - targets:
          - user-service:8080
